'use client';
import React, {useEffect} from "react";

export default function UserPage({params}){
    const { id } = React.use(params);
    useEffect(() => {
        const [result]
    }, []);
    return(
        <div>

        </div>
    )
}